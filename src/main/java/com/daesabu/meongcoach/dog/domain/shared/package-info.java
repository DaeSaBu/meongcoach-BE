/**
 * dog 모듈이 다른 모듈과 공유하는 도메인 타입. 값 자체가 계약이라 provided record로 복사하지 않고 그대로 노출한다.
 */
@NamedInterface("shared")
package com.daesabu.meongcoach.dog.domain.shared;

import org.springframework.modulith.NamedInterface;
