package com.daesabu.meongcoach.training.domain;

/**
 * 커리큘럼 노출 상태. ERD상 DEFAULT TRUE로 표기된 boolean 성격의 컬럼이라 ACTIVE/INACTIVE로 정정해 정의했다 — 기획 확인 필요.
 */
public enum CurriculumStatus {
	ACTIVE,
	INACTIVE,
}
